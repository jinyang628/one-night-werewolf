module.exports = {
  // The simulator can reach localhost. Replace this with the deployed HTTPS
  // API URL before previewing on a phone or submitting to WeChat.
  apiBaseUrl: 'http://127.0.0.1:8080/api/v1',
};
